\ProvidesPackage{myhyperref}
\RequirePackage[
    hyperfootnotes=false,
]{hyperref}

\hypersetup{%
    pdftitle={%
        Supplementary wavelength calibration methods for SALT/RSS spectropolarimetric observations
    },
    pdfauthor={%
        Justin Cooper
    },
    pdfsubject={%
        POLSALT alternate wavelength calibrations, created via IRAF and Python; parsing and validation handled via STOPS.
    },
    pdfkeywords={%
        STOPS,
        Supplementary Tools for Polsalt Spectropolarimetry,
        SALT,
        Southern African Large Telescope,
        POLSALT,
        Polarimetric reductions for SALT,
        RSS,
        Robert Stobie Spectrograph,
        IRAF,
        Image Reduction and Analysis Facility,
        Python,
        Wavelength Calibration,
        Spectropolarimetry,
        Astrophysics,
        Astronomy,
    },
    pdfstartview=Fit,
}

% Redefine \autoref{} defaults
% (see `texdoc hyperref` for more information)
\renewcommand{\sectionautorefname}{\S}
\renewcommand{\subsectionautorefname}{\S}
\renewcommand{\subsubsectionautorefname}{\S}

% \renewcommand{\lstlistingautorefname}{Listing}
% \providecommand*{\lstnumberautorefname}{line}
